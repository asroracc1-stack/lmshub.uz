package com.lmscrm.backend.bot.enums;

public enum BotState {
    START,
    AWAITING_NAME,
    AWAITING_PHONE,
    MAIN_MENU,
    AWAITING_CATEGORY,
    AWAITING_PAYMENT_RECEIPT,
    AWAITING_REVIEW,
    AWAITING_ADMIN_LOGIN
}
